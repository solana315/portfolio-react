const HeroQuote = () => (
  <div className="row mt-3 mt-xl-4">
    <div className="col-12 d-flex justify-content-end">
      <blockquote className="hero-quote mb-0">“Make space for good ideas.”</blockquote>
    </div>
  </div>
);

export default HeroQuote;
